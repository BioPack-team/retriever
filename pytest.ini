[pytest]
markers =
    live: tests that perform live requests (skipped by default)
    auth: tests that require authentication (skipped by default)
# Skip live and auth tests by default
addopts = -m "not live and not auth"